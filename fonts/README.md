# Solea

`solea-regular.ttf` is the same Solea Regular (400, normal) font used by the
Be Art Next.js site's manifesto page, copied from
`beartgroup/site/app/fonts/solea-regular.ttf`.

The Circle manifesto title loads it locally, with no dependency on the
Next.js development server.
